name: Sync multiple branches
on:
  push:
    branches:
      - main
jobs:
  sync-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Merge main -> arch
        uses: devmasx/merge-branch@master
        with:
          type: now
          from_branch: main
          target_branch: arch
          github_token: ${{ github.token }}
